import React from "react";
import Layout from "../src/components/Layout";

const meetings = () => {
  return (
    <Layout>
      <h1 className="mainTitle">Meetings section</h1>
    </Layout>
  );
};

export default meetings;
