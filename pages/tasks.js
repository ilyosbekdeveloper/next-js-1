import React from "react";
import Layout from "../src/components/Layout";

const tasks = () => {
  return (
    <Layout>
      <h1 className="mainTitle">Tasks section</h1>
    </Layout>
  );
};

export default tasks;
