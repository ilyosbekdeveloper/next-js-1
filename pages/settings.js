import React from "react";
import Layout from "../src/components/Layout";

const settings = () => {
  return (
    <Layout>
      <h1 className="mainTitle">Settings section</h1>
    </Layout>
  );
};

export default settings;
